<h1>Index</h1>


<main>
  <% @trips.each do |trip| %>
    <%= trip.title %> 
    <%= trip.duration %> 
    <%= tweet.startdate %> 
    <%= tweet.enddate %> 
    <%= tweet.price %> 
  <% end %>
</main>