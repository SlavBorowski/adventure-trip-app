<h1>Index</h1>


<main>
  <% @trips.each do |trip| %>
    Trip: </h4><%= trip.title %> 
    Duration: <%= trip.duration  %> <%=" day".pluralize(trip.duration)%>
    Trip begins: <%= trip.startdate %> 
    Trip ends: <%= trip.enddate %> 
    Price: $<%= trip.price %> 
  <% end %>
</main>